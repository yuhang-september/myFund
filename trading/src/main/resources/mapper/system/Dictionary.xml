<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuhang.trading.mapper.system.DictionaryMapper">

    <resultMap id="dictionary" type="com.yuhang.trading.entity.system.Dictionary">
        <id column="id" property="id" javaType="String" jdbcType="CHAR"/>
        <result column="name" property="name" javaType="String" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insert" parameterType="com.yuhang.trading.entity.system.Dictionary">
        insert into sys_dictionary (id, name) value (
                                                     #{dictionary.id}, #{dictionary.name}
            )
    </insert>

    <select id="selectById" resultMap="dictionary">
        SELECT id, name
        FROM sys_dictionary
        where id = #{id}
    </select>

    <select id="selectAll" resultType="com.yuhang.trading.entity.system.Dictionary">
        select `id`, `name`
        from sys_dictionary
    </select>
</mapper>