<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuhang.trading.mapper.buy.BuyMapper">

    <sql id="allColumns">
        trade_request_id,
        fund_code,
        application_amount,
        status,
        confirm_status,
        confirm_amount,
        confirm_share,
        create_time,
        last_update_time
    </sql>

    <insert id="insert" parameterType="com.yuhang.trading.entity.request.TradeRequest">
        insert into tr_trade_request (<include refid="allColumns"/>) value (
        #{request.tradeRequestId},#{request.fundCode},#{request.applicationAmount},#{request.status},
        #{request.confirmStatus},#{request.confirmAmount},#{request.confirmShare},#{request.createTime},
        #{request.lastUpdateTime}
        )
    </insert>

</mapper>